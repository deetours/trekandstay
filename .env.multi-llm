# OpenRouter Configuration
OPENROUTER_API_KEY=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

# Multi-LLM Configuration
MULTI_LLM_CONFIG = {
    'writer': {
        'model': 'kimi-k-1.5',
        'purpose': 'Sales Response Writing',
        'free_tier': True,
    },
    'analyzer': {
        'model': 'qwen-2-7b-instruct',
        'purpose': 'Intent Analysis',
        'free_tier': True,
    },
    'tracker': {
        'model': 'deepseek-chat',
        'purpose': 'Context Tracking',
        'free_tier': True,
    },
    'strategist': {
        'model': 'grok-4',
        'purpose': 'Strategic Decisions',
        'free_tier': True,
    }
}

# Custom WhatsApp API (not Meta Business API)
CUSTOM_WHATSAPP_ENDPOINT=http://localhost:8001/api/whatsapp
CUSTOM_WHATSAPP_API_KEY=travel-bot-secret-key-2024
CUSTOM_WHATSAPP_API_SECRET=webhook-shared-secret-123

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
CELERY_ACCEPT_CONTENT=['json']
CELERY_TASK_SERIALIZER='json'
CELERY_RESULT_SERIALIZER='json'
CELERY_TIMEZONE='UTC'

CELERY_BEAT_SCHEDULE = {
    'zero-human-automation': {
        'task': 'backend.services.zero_human_automation.run_zero_human_automation',
        'schedule': 1800,  # 30 minutes
    },
    'process-pending-messages': {
        'task': 'backend.services.zero_human_automation.process_pending_messages',
        'schedule': 300,  # 5 minutes
    },
}

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/trek_stay

# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1